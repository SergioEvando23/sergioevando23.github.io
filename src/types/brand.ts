export interface BrandConfig {
  name: string;
  initials: string;
  role: string;
  description: string;
  location: string;
  email: string;
  socialLinks: {
    github: string;
    linkedin: string;
  };
}
